# Run Playwright Tests
1. Run the full Playwright test suite: `npx playwright test`
2. If tests fail, diagnose and fix locator/selector issues
3. Re-run only failing tests to verify fixes
4. Run full suite once more to confirm no regressions
5. Summarize: tests passed, tests fixed, remaining issues
